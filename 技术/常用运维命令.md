[TOC]
# 常用运维命令
```shell
htop
history

service mysqld restart
set password for `root`@`localhost` = password('junjun');
ps aux | grep rails
kill -9 21352
rails s proudction -p 80
netstat -ntpl
ps
bundle install
```